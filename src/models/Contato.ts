class Contato {
  titulo: string
  descricao: string
  id: number

  constructor(titulo: string, descricao: string, id: number) {
    this.titulo = titulo
    this.descricao = descricao
    this.id = id
  }
}

export default Contato
